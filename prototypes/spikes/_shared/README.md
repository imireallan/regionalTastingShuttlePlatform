# Shared Spike Notes

Keep this folder small. Shared code is allowed only when it prevents repeated boring setup and does not become production architecture.

Allowed:

- sample route/stops
- fake movement generator
- report templates

Not allowed:

- production app shell
- reusable feature modules
- auth abstractions
- deployment assumptions
